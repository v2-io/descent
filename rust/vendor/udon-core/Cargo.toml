# VENDORED stage-0 snapshot — see PROVENANCE.md in this directory.
# Do not edit src/; bump via the procedure in PROVENANCE.md.
[package]
name = "udon-core"
version = "0.3.0-stage0"
edition = "2021"
license = "MIT"
publish = false
description = "Vendored stage-0 UDON parser (generated by Ruby descent) — libdescent front-end"

[dependencies]
unicode-xid = "0.2"
memchr = "2.7"
phf = { version = "0.11", features = ["macros"] }

[features]
default = ["std"]
std = []

# Generated parser.rs carries a Ruby-descent case-overlap defect (ledgered);
# silence it here rather than editing the generated file.
[lints.rust]
unreachable_patterns = "allow"
